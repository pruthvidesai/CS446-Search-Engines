README
Pruthvi Desai

No dependencies needed except a python 2.7 install
There is not need to build the code, running is building in python
You can execute the code by typing "python programname argument" in terminal or cmd prompt
 